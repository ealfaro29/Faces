export default {
    content: [
        "./index.html",
        "./**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
        extend: {
            colors: {
                // Keep your custom colors here if needed, or rely on CSS variables
            }
        },
    },
    plugins: [],
}
